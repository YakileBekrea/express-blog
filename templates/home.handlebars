<!DOCTYPE html>
<head>
    
</head>
<body>
    {{#each posts}}
    <h2>
        {{title}}
    </h2>
    <p class="PostContent">
        {{content}}
    </p>
    <div class="PostTime">
        {{time}}
    </div>
    {{/each}}
</body>