<!DOCTYPE html>
<head>
    
</head>
<body>
    {{#each post}}
    <h2>
        {{title}}
    </h2>
    <p class="PostContent">
        {{content}}
    </p>
    <div class="PostTime">
        {{time}}
    </div>
    {{/each}}
    <div>
        {{params.id}}
        {{id}}
    </div>
</body>